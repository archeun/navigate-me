'use strict';
const consts = {
  storage: {
    keys: {
      VISITED_URLS: 'visited_urls',
      SETTINGS: {
        MAIN: 'settings',
        RECORD_NAVIGATIONS: 'record-navigations-check',
      },
    },
  },
};

module.exports = consts;
